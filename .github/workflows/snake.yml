name: Generate Snake Animation

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Generate snake
        uses: Platane/snk@master
        with:
          github_user_name: iamksr05
          outputs: |
            dist/github-contribution-grid-snake.svg?color=random-dark
      
      - name: Commit to output branch
        run: |
          git config user.name github-actions[bot]
          git config user.email github-actions[bot]@users.noreply.github.com
          
          # Check if output branch exists remotely, if not create it locally first
          git fetch origin output:output 2>/dev/null || true
          
          # Try to checkout output branch, create if it doesn't exist
          git checkout output 2>/dev/null || git checkout --orphan output
          
          # Copy the generated file from dist/ to the root of output branch
          cp dist/github-contribution-grid-snake.svg github-contribution-grid-snake.svg
          
          # Commit and push
          git add github-contribution-grid-snake.svg
          git commit -m "Update snake animation [skip ci]" || exit 0
          git push origin output || git push -u origin output
